<!DOCTYPE html>
<html>
<head>
  <title>survey</title>
  <script src="https://ajax.googleapis.com/ajax/libs/jquery/1.12.4/jquery.min.js"></script>
  <script src="https://ajax.googleapis.com/ajax/libs/jquery/2.2.4/jquery.min.js"></script>

  <script>
    $(document).ready(function(){
      $('#browserother').hide();
      $('form div select[name=campus]').change(function(e){
        if ($('form div select[name=campus]').val() == 'other'){
          $('#browserother').show();
        }else{
          $('#browserother').hide();
        }
      });
    });
  </script>

</head>
<body>
  <form action="/campuses" method="POST">
    <div>
      <label>Choose Campus</label>
      <select name ="campus">
        <% campuses = @campuses.sort_by {|key| key["campus"]} %>
        <% campuses.each do |campus| %>
        <% p campus %>
        <option value="<%= campus['campus'] %>"><%= campus['campus'] %></option>
        <% end %>
        <option value="other">Other</option>
      </select>
    </div>

    <div id="browserother">
      <label>Campus</label>
      <input type="text" name="campus" placeholder="Type Campus Here">
    </div>
    
    <div>
      <button type="submit">Enter</button>
    </div>
  </form>

</body>
</html>